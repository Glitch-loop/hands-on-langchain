# This file contains the generated prompt template for extracting GeneralInvoiceInformation fields.
# The template is stored as a JSON dictionary for easy reuse.

import json

prompt_template = '''
{template}
'''

with open("prompt_template.json", "w", encoding="utf-8") as f:
    f.write(prompt_template)
